'use strict';

/**
 * 
 * @param userid Platform specific identifier string for a user
 * @param platform Name of the platform ('fb', 'slack')
 */
function getSuggestion(userid, platform) {

    // Get user profile

    // Get group of ASINs from most popular browse node for the user

    // Return item suggestions using SimilarityLookup
};

module.exports = getSuggestion;